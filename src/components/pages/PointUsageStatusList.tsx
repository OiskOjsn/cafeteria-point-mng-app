import React, { memo, VFC } from "react";

import { PointUsageTable } from "../organisms/table/PointUsageTable";

export const PointUsageStatusList: VFC = memo(() => {
  return (
    <PointUsageTable />
  )
});
